# History extension

Custom history extension because CodeMirror's built-in history extension doesn't support tree-based undo/redo nor does it track changes to some annotations/decorations. Thus this is a modified version of CodeMirror's history extension.
