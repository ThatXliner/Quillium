---
phase: 08.5a-crdt-data-shape
verified: 2026-04-18T15:20:00Z
status: passed
score: 4/4 must-haves verified
overrides_applied: 0
---

# Phase 08.5a: CRDT Data Shape Verification Report

**Phase Goal:** Rewrite the YjsAnnotation data model from a flat JSON blob into a recursive Y.Map node with Y.Text/Y.Array/Y.Map children, so every revision version owns a dedicated Y.Text subtree.
**Verified:** 2026-04-18T15:20:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `YjsAnnotationNode = Y.Map<unknown>` replaces the legacy flat `YjsAnnotation` interface in `src/lib/collab/types.ts` | VERIFIED | `types.ts:33` exports `export type YjsAnnotationNode = Y.Map<unknown>`. `grep "export interface YjsAnnotation"` in types.ts returns no matches — legacy interface removed. `CollabSession.ymap` retyped to `Y.Map<YjsAnnotationNode>` (`types.ts:53`). `MessageObject` interface owned by collab layer (`types.ts:39-43`). |
| 2 | `codeMirrorToYjsAnnotation` builds child Y types (Y.Text for version bodies, Y.Array for threads, Y.Map for nested annotations) inside a single `ydoc.transact` | VERIFIED | `annotationSchema.ts:49-102` — signature is 4-arg `(annotation, ytext, clientId, ydoc)`. Single `ydoc.transact(..., "init")` block at line 58 wraps all child creation: `new Y.Array<MessageObject>` (thread, line 64), `new Y.Map<YjsAnnotationNode>` (annotations, line 70), `new Y.Array<SuggestionReplacement>` (replacements, line 73), `new Y.Map<Y.Map<unknown>>` (versions, line 82), per-version `new Y.Map<unknown>` + `new Y.Text()` seeded with `v.doc` (lines 84-88). |
| 3 | `yjsAnnotationToCodeMirror` reads Y.Map children directly with no JSON parsing (no JSON.stringify / JSON.parse / safeJsonParse in annotationSchema.ts) | VERIFIED | `grep "JSON\.\|safeJsonParse" src/lib/collab/annotationSchema.ts` returns nothing. Reader uses direct `instanceof Y.Array / Y.Map / Y.Text` guards (lines 118, 135, 152, 165, 180) and `threadArr.toArray()` / `replArr.toArray()` / `vtext.toString()` — no JSON boundary. `safeJsonParse` helper and `YjsAnnotationSchema` zod schema fully deleted. |
| 4 | `annotation-tree.test.ts` passes shape-integrity, version-propagation, thread-ordering, bounds-clamp, and null-selection tests | VERIFIED | `bun run test:run src/lib/collab/annotation-tree.test.ts` → **5 passed, 1 todo, 0 failed** (the 1 todo is "observeDeep fires for descendant Y.Text changes" explicitly deferred to Plan 8.5b-01). All five required test names are present and passing: "recursive shape integrity", "version propagation", "roundtrip preserves thread ordering", "revision clamps out-of-range activeVersionIndex", "null selection when anchored text deleted". |

**Score:** 4/4 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/lib/collab/types.ts` | Exports `YjsAnnotationNode`, `MessageObject`; `CollabSession.ymap` retyped; legacy `YjsAnnotation` interface removed | VERIFIED | Exists, 77 lines, exports all three symbols. Legacy interface absent (grep returns nothing for `YjsAnnotation[^N]`). File header updated to cite D-90/D-92/D-93. |
| `src/lib/collab/annotationSchema.ts` | Exports rewritten `codeMirrorToYjsAnnotation` (4-arg: ydoc), `yjsAnnotationToCodeMirror`, preserves `AnnotationIdMap`/`generateAnnotationId`; removes `safeJsonParse` and `YjsAnnotationSchema` | VERIFIED | Exists, 250 lines. `codeMirrorToYjsAnnotation` signature: `(annotation, ytext, clientId, ydoc)`. `yjsAnnotationToCodeMirror` signature: `(node, ydoc, ytext, numericId)`. `AnnotationIdMap` class preserved verbatim (lines 205-249). No Zod import, no `YjsAnnotationSchema`, no `safeJsonParse`. |
| `src/lib/collab/annotation-tree.test.ts` | 5 green tests (shape, propagation, ordering, clamp, null-selection) + 1 remaining todo | VERIFIED | 259 lines. 5 `it(...)` passing + 1 `it.todo("observeDeep fires for descendant Y.Text changes")`. Uses `buildAndIntegrate` helper to avoid the detached-Y-Map prelim-content gotcha. Version-propagation test uses standalone two-Y.Doc setup (bypassing the legacy-shape `createAnnotationSyncPlugin` in `yjsAnnotations.ts`). |
| `src/lib/collab/test-helpers/twoPeerHarness.ts` | Exports `makePeer`, `connect`, `teardown`, `Peer` for convergence suites (Plan 8.5a-01 scope) | VERIFIED | 85 lines, 4 exports confirmed. Imports `createYjsBinding` and `createAnnotationSyncPlugin`. Uses `"init"` and `"remote"` origin tags. `ymap` intentionally typed `Y.Map<unknown>` to survive the Plan 8.5a-02 shape rewrite. |
| Wave 0 test scaffolds (8 files) | `subtree-binding.test.ts`, `undo-manager.test.ts`, `annotation-tree.test.ts`, `version-coordination.test.ts`, `subtree-convergence.test.ts`, `convergence-edgecases.test.ts`, `thread-append.test.ts`, `recursive-mount.test.ts` | VERIFIED | All 8 files exist per `ls src/lib/collab/*.test.ts`. Plan 8.5a-01 SUMMARY confirms 19 `it.todo` entries wired to per-plan `-t "<pattern>"` targets. Plan 8.5a-02 converted 5 of these to real assertions in `annotation-tree.test.ts`. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `annotationSchema.ts` | `types.ts` | `import type { YjsAnnotationNode, MessageObject }` | WIRED | `annotationSchema.ts:28` imports both types from `./types`. |
| `annotationSchema.ts::codeMirrorToYjsAnnotation` | Y.Map / Y.Text / Y.Array constructors | `new Y.Map / new Y.Text / new Y.Array` inside a single `ydoc.transact(..., "init")` | WIRED | Single `ydoc.transact` block at line 58 contains 8 `new Y.*` constructions: 1 Y.Array (thread), 1 Y.Map (annotations), 1 Y.Array (replacements, suggestion path), 1 Y.Map (versionsMap, revision path), N Y.Map per-version, N Y.Text per-version, N Y.Map per-version annotations subtree. All wrapped with the `"init"` origin tag. |
| `types.ts::MessageObject` | `$lib/editor/plugins/annotations/models::ThreadMessage` | Identical field contract `{message, author, time}` | WIRED | `MessageObject` interface at `types.ts:39-43` has exactly `{message: string, author: string, time: number}` — matches `ThreadMessage` from models.ts. Collab layer owns its copy to avoid circular import. `Y.Array<MessageObject>.toArray()` result flows straight into CM `Thread` with no translation layer (`annotationSchema.ts:135-136`). |
| `annotation-tree.test.ts` | `annotationSchema.ts` | `import { codeMirrorToYjsAnnotation, yjsAnnotationToCodeMirror }` | WIRED | Test imports both converters at line 20 and exercises them across all 5 passing tests. |
| `annotation-tree.test.ts` | `types.ts` | `import type { YjsAnnotationNode }` | WIRED | Line 22 imports `YjsAnnotationNode`; used as the type parameter for host `Y.Map` in `makeHost` helper and return type of `buildAndIntegrate`. |

### Data-Flow Trace (Level 4)

Not applicable — this phase delivers pure data-shape + converter logic. No UI artifacts render dynamic data in this phase. Downstream consumers (NestedEditorController, yjsAnnotations plugin) are explicitly deferred to Plans 8.5b-01 and 8.5c-01 per the phase split. Tests directly exercise data flow: `codeMirrorToYjsAnnotation` → integrated Y.Map → `yjsAnnotationToCodeMirror` roundtrips are verified for versions (text field propagates), thread ordering, activeVersionIndex clamping, and null-on-unreachable-anchor.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| annotation-tree test suite green | `bun run test:run src/lib/collab/annotation-tree.test.ts` | 5 passed, 1 todo (expected), 0 failed | PASS |
| No legacy YjsAnnotation interface in types.ts | `grep "export interface YjsAnnotation" src/lib/collab/types.ts` | empty | PASS |
| No JSON stringification in converter | `grep "JSON\.\|safeJsonParse" src/lib/collab/annotationSchema.ts` | empty | PASS |
| Single ydoc.transact wrapping child Y-type creation | `grep -n "ydoc\.transact" src/lib/collab/annotationSchema.ts` | 1 runtime call at line 58 (+ 2 doc-comment references at lines 10, 46) | PASS |
| All 8 child Y constructors inside converter | `grep -n "new Y\.(Map\|Text\|Array)" src/lib/collab/annotationSchema.ts` | 8 matches, all inside the transact block | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| SYNC-05 | 08.5a-01, 08.5a-02 | "Annotations (comments, revisions, suggestions) sync as part of document state" — Phase 8.5a delivers the recursive Y.Map data-shape slice; sync plumbing (8.5b) and nested-editor wiring (8.5c) land in subsequent sub-phases | SATISFIED (partial progress, as expected) | Phase 8.5a explicitly marked in ROADMAP as "SYNC-05 (extended — Phase 8.5a delivers the data-shape slice; remaining slices land in 8.5b and 8.5c)". Data-shape requirements complete: recursive Y.Map node + Y.Text version subtrees + Y.Array threads, all covered by annotation-tree.test.ts. Full SYNC-05 closure tracked across 8.5a → 8.5b → 8.5c per ROADMAP.md phase split. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (none) | — | — | — | — |

No TODO/FIXME/HACK/PLACEHOLDER markers in `types.ts`, `annotationSchema.ts`, or `annotation-tree.test.ts`. The `it.todo("observeDeep fires for descendant Y.Text changes")` in `annotation-tree.test.ts` is explicitly deferred to Plan 8.5b-01 per the plan's own success criteria and is recorded as a vitest todo (not a code-level anti-pattern). Broken type imports in `yjsAnnotations.ts`, `yjsUndo.ts`, `yjsProvider.ts`, `index.ts`, `annotationSchema.test.ts`, `yjsAnnotations.test.ts`, `yjsAnnotations.convergence.test.ts`, `types.test.ts` are explicitly out-of-scope per the prompt's `<expected_out_of_scope>` block and are already tracked as owned by Plan 8.5b-01 in 08.5a-02-SUMMARY.md.

### Human Verification Required

None. All four success criteria are programmatically verifiable and pass. The `annotation-tree.test.ts` suite exercises the converter across all specified scenarios (shape, propagation, ordering, clamp, null-selection). No visual/UX/runtime behavior to evaluate at this phase — downstream user-visible behavior (subtree character-merge, undo auto-nav) is explicitly deferred to 8.5b/8.5c.

### Gaps Summary

No gaps. Phase 08.5a delivers exactly the data-shape slice promised by its goal:

- Legacy flat `YjsAnnotation` interface is gone from `types.ts`; `YjsAnnotationNode = Y.Map<unknown>` is now the canonical Yjs-side shape.
- `codeMirrorToYjsAnnotation` constructs every child Y-type (Y.Text version bodies, Y.Array threads, nested Y.Map for sub-annotations, versions-as-Y.Map) atomically inside a single `ydoc.transact(..., "init")` so remote peers never observe a partial node.
- `yjsAnnotationToCodeMirror` reads recursive Y.Map children directly via `instanceof` guards; `safeJsonParse` and `YjsAnnotationSchema` zod schema are fully removed; the converter is the validation boundary.
- `annotation-tree.test.ts` reports 5 passed / 1 todo / 0 failed; the one todo is the `observeDeep` check explicitly owned by Plan 8.5b-01.
- Wave 0 scaffolding from Plan 8.5a-01 (twoPeerHarness + 8 test files with 19 `it.todo` entries) is in place for downstream plans to replace.

Expected broken callers (`yjsAnnotations.ts`, `yjsUndo.ts`, `yjsProvider.ts`, collab `index.ts`, plus the four downstream test files) still reference `YjsAnnotation` / the 3-arg converter / `YjsAnnotationSchema`; these are documented scope boundaries of Plans 8.5b-01 and 8.5c-01 and are not counted as gaps per the prompt's `<expected_out_of_scope>` block.

---

*Verified: 2026-04-18T15:20:00Z*
*Verifier: Claude (gsd-verifier)*
