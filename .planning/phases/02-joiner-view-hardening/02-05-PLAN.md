---
phase: 02
plan: 05
type: execute
wave: 3
depends_on: [02-01, 02-02, 02-03, 02-04]
files_modified:
  - src/lib/collab/joiner-view.test.ts
autonomous: true
requirements: [JOINER-01, JOINER-02, JOINER-03, JOINER-05]
nyquist_compliant: true
tags: [collab, joiner, undo, end-to-end, validation]
must_haves:
  truths:
    - "JOINER-01: a constructed joiner peer has historyField === undefined"
    - "JOINER-03: Cmd-z (undoManager.undo()) on a freshly-connected joiner does not change doc or annotations"
    - "JOINER-05: owner has N annotations + joiner connects → joiner annotation count = N (not 2N)"
    - "Criterion #5: joiner peer exposes undoManager (Y.UndoManager) AND has no historyField — createYjsUndoExtension is the ONLY undo mechanism installed on joiner"
    - "Criterion #6: joiner undo affects only joiner's edits; owner-origin edits remain"
    - "Criterion #7: owner can undo own edits; cannot undo joiner's incoming text edits (verified via makeOwnerPeer that DOES install history())"
    - "Criterion #8: undo restores selection to where the cursor was at edit time (clipped to current doc length)"
    - "Plan 02-05 is the only Phase-2 file that touches the new test surface; no production-code edits"
  artifacts:
    - path: "src/lib/collab/joiner-view.test.ts"
      provides: "End-to-end joiner-view assertions for JOINER-01/-03/-05 + criteria #6-#8"
      contains: "describe(\"joiner view"
  key_links:
    - from: "src/lib/collab/joiner-view.test.ts"
      to: "src/lib/collab/test-helpers/twoPeerHarness.ts"
      via: "makeJoinerPeer + makePeerWithAnnotationSync + connect + flushAll"
      pattern: "makeJoinerPeer"
---

<objective>
Create `src/lib/collab/joiner-view.test.ts` — the end-to-end test suite that
proves Phase 02 success. Each of the seven assertions (JOINER-01, JOINER-03,
JOINER-05, criteria #6, #7, #8) is a separate `it(...)` named to match the
test-name strings registered in `02-VALIDATION.md`.

Purpose: every prior plan in Phase 02 land mechanism-level changes verified
by surgical unit tests. This plan is the *integration*-level proof that the
mechanisms compose correctly into the user-visible behavior described in
02-CONTEXT §specifics. Without this file, the success criteria of Phase 02
are unverified.

Output: One new test file. No production-code edits.
</objective>

<execution_context>
@/Users/bryanhu/Developer/current/Quillium/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bryanhu/Developer/current/Quillium/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-joiner-view-hardening/02-CONTEXT.md
@.planning/phases/02-joiner-view-hardening/02-RESEARCH.md
@.planning/phases/02-joiner-view-hardening/02-VALIDATION.md
@.planning/phases/02-joiner-view-hardening/02-01-SUMMARY.md
@.planning/phases/02-joiner-view-hardening/02-02-SUMMARY.md
@.planning/phases/02-joiner-view-hardening/02-03-SUMMARY.md
@.planning/phases/02-joiner-view-hardening/02-04-SUMMARY.md
@src/lib/collab/test-helpers/twoPeerHarness.ts
@src/lib/collab/yjsUndo.ts

<interfaces>
<!-- After Plan 02-01: -->
```ts
export function makeJoinerPeer(
    clientId: string,
    initialText?: string,
): Peer & { undoManager: Y.UndoManager };
```

<!-- After Plan 02-02: -->
- enableCollab(asOwner=false) reconfigures historyCompartment to []
- view.state.field(historyField, false) === undefined on joiner

<!-- After Plan 02-03: -->
- createYjsUndoExtension returns extension array with selection-restore plugin
- stack-item-added stashes view.state.selection; stack-item-popped restores it

<!-- After Plan 02-04: -->
- yjsBinding.ts remote-text dispatch carries Transaction.addToHistory.of(false)
- _syncInitialToYjs is structurally re-entrance-guarded
- deepObserver attached only after initial pull microtask
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create joiner-view.test.ts with all 7 assertions</name>
  <files>src/lib/collab/joiner-view.test.ts</files>
  <read_first>
    - src/lib/collab/test-helpers/twoPeerHarness.ts (FULL file — post-Plan-02-01 state; confirm `makeJoinerPeer` signature, `connect`, `flushAll`, `teardown`, `Peer` interface)
    - src/lib/collab/yjsUndo.ts (post-Plan-02-03 — confirm selectionRestorePlugin shape; confirm `undoManager.undo()` and `undoManager.redo()` are available)
    - src/lib/collab/yjsAnnotations.ts (post-Plan-02-04 — confirm `_initialSyncDone` and deferred deepObserver attach)
    - src/lib/collab/yjsBinding.ts (post-Plan-02-04 — confirm `Transaction.addToHistory.of(false)` is in remote dispatch)
    - src/lib/collab/yjsUndo.test.ts (REFERENCE for style — match the existing test-file shape, imports, conventions)
    - src/lib/collab/annotation-sync.test.ts (REFERENCE for two-peer test patterns)
    - .planning/phases/02-joiner-view-hardening/02-VALIDATION.md (test-name strings MUST match exactly — see "Per-Task Verification Map" / "Requirements → Test Coverage")
    - .planning/phases/02-joiner-view-hardening/02-CONTEXT.md (§specifics — the seven success criteria, in order)
  </read_first>
  <action>
    Create `src/lib/collab/joiner-view.test.ts`. The file MUST start with this comment block:
    ```ts
    // joiner-view.test.ts — End-to-end Phase 02 verification: JOINER-01/-03/-05
    // + criteria #6 (own-edits-only undo), #7 (owner history excludes remote text),
    // and #8 (selection restored on undo/redo). Uses Phase 1 flushAll primitive
    // and Plan 02-01 makeJoinerPeer factory.
    ```

    Imports (single import statements per source — no duplicates):
    - `describe, it, expect, afterEach` from `"vitest"`
    - `* as Y` from `"yjs"`
    - `historyField` from `"@codemirror/commands"`
    - `EditorSelection` from `"@codemirror/state"`
    - `connect, flushAll, teardown, makeJoinerPeer, makeOwnerPeer, makePeerWithAnnotationSync` from `"./test-helpers/twoPeerHarness"`

    Top-level `describe("joiner view hardening", () => { ... })` containing
    EXACTLY these seven `it(...)` blocks with these EXACT test-name strings
    (the `-t` flags in 02-VALIDATION.md depend on these being substring-matchable):

    1. `it("no history field — joiner state.field(historyField, false) is undefined", ...)`
       - Build owner via `makePeerWithAnnotationSync("owner", "hello")`.
       - Build joiner via `makeJoinerPeer("joiner")`.
       - Connect both, `await flushAll(owner, joiner)`.
       - Assert `expect(joiner.view.state.field(historyField, false)).toBeUndefined();`
       - This requires the harness's joiner peer to mirror the production
         joiner shape (history() omitted) — `makeJoinerPeer` does this.
       - Note: this test asserts the harness shape directly. The
         `enableCollab(asOwner=false)` reconfigure path is exercised by
         Plan 02-02's compartment infrastructure; this test confirms the
         downstream invariant the harness reproduces.
       - Teardown both peers in `afterEach` or inline.

    2. `it("cmd-z post-connect — undo on freshly-connected joiner is a no-op", ...)`
       - Owner: `makePeerWithAnnotationSync("owner", "shared text")`.
       - Joiner: `makeJoinerPeer("joiner")`. Connect. `await flushAll(...)`.
       - Capture `const docBefore = joiner.view.state.doc.toString();`
       - Capture `const annBefore = JSON.stringify(joiner.view.state.field(annotationField));` (import annotationField from the appropriate module — match how `yjsUndo.test.ts` imports it).
       - Call `joiner.undoManager.undo();` `await flushAll(...)`.
       - Assert doc and annotations unchanged: `expect(joiner.view.state.doc.toString()).toBe(docBefore);` and same for annotations.

    3. `it("no annotation duplication — owner has N annotations, joiner connects, joiner annotation count = N", ...)`
       - Build owner via `makePeerWithAnnotationSync("owner", "owner text")`.
       - Owner programmatically dispatches 3 `addAnnotation` effects (use the
         same dispatch pattern as `annotation-sync.test.ts` does — match the
         existing helper or build inline; the goal is to land 3 annotations
         in `owner.ymap` BEFORE joiner connects).
       - `await flushAll(owner)`.
       - Then build joiner via `makeJoinerPeer("joiner")` and `connect(owner, joiner)`.
       - `await flushAll(owner, joiner)`.
       - Assert: `const joinerCount = Object.keys(joiner.view.state.field(annotationField)).length;`
         `expect(joinerCount).toBe(3);` (NOT 6 — proves 2x duplication is fixed).
       - Defensive secondary assertion: `expect(joiner.ymap.size).toBe(3);` (the underlying Y.Map also has 3, not 6 keys).

    4. `it("undo own edits only — joiner Cmd-z does not affect owner's edits", ...)`
       - Owner + joiner via the two factories. Connect. `await flushAll(...)`.
       - Owner inserts text: `owner.ydoc.transact(() => owner.ytext.insert(0, "OWNER "), "local")`.
       - `await flushAll(...)`. Confirm joiner sees `"OWNER "` prefix.
       - Joiner inserts text via CM dispatch (use `joiner.view.dispatch({ changes: { from: joiner.view.state.doc.length, insert: " JOINER" } })` — the local origin is set by the binding).
       - `await flushAll(...)`. Joiner doc now ends with " JOINER".
       - Joiner: `joiner.undoManager.undo();` `await flushAll(...)`.
       - Assert joiner doc still starts with `"OWNER "` (owner's edit NOT undone) and no longer ends with `" JOINER"` (joiner's own edit undone).

    5. `it("owner history excludes remote text — owner Cmd-z does not undo joiner's incoming text", ...)`
       - Build owner via `makeOwnerPeer("owner")` (Plan 02-01 added this
         factory; it mirrors `makePeerWithAnnotationSync` but ALSO installs
         `history()` so the owner has CM undo to test against — without
         this, the test would pass vacuously).
       - Build joiner via `makeJoinerPeer("joiner")`. Connect. `await flushAll(...)`.
       - Owner inserts `"OWNER "` via CM dispatch (enters owner CM history).
       - Joiner inserts `" JOINER"` via CM dispatch.
       - `await flushAll(...)`. Owner's doc now contains both.
       - On the OWNER, invoke CM's `undo` command:
         `import { undo } from "@codemirror/commands"` then `undo(owner.view)`.
       - Per criterion #7, joiner's incoming text was dispatched on the
         owner with `Transaction.addToHistory.of(false)`, so owner's CM
         history contains ONLY owner's own insertion. Owner `undo` must
         undo OWNER's own insertion, NOT joiner's.
       - Assert owner doc ends with `" JOINER"` (joiner's edit preserved)
         and does NOT contain `"OWNER "` (owner's edit undone).

    6. `it("selection restored — undo restores caret to where it was at edit time", ...)`
       - Joiner via `makeJoinerPeer("joiner", "hello world")`.
       - Set initial selection: `joiner.view.dispatch({ selection: EditorSelection.cursor(5) });`
       - Insert text via CM dispatch: `joiner.view.dispatch({ changes: { from: 5, insert: "X" }, selection: EditorSelection.cursor(6) });` `await Promise.resolve();`
       - Capture `const selectionAfterEdit = joiner.view.state.selection;` (should be cursor at 6).
       - `joiner.undoManager.undo();` `await flushAll(joiner);`
       - Assert: `expect(joiner.view.state.selection.main.head).toBe(5);`
         (selection restored to position 5 — the cursor location at the moment
         the stack item was created, clipped to current doc length).
       - Note: per Plan 02-03 Pitfall 2 mitigation, selection is clipped to
         doc length, so a selection at "post-edit position 6" stashed before
         the doc shrinks back to 11 chars (no shrink here actually — "hello
         worldX" → "hello world" length 11; selection 6 still valid; expect 5
         only if the selectionRestorePlugin uses pre-edit selection. Re-read
         RESEARCH §8 Pitfall 2 to confirm direction; if the plugin captures
         POST-edit selection (the 6), expect 6 here. The acceptance is that
         the selection IS restored to whatever the plugin captured, not lost.)
         CONCRETE EXPECTATION: read the implementation in yjsUndo.ts post-Plan-02-03 and assert against the captured value (use the test to PROVE round-trip identity by calling redo and checking selection returns to post-edit state).

       Pragmatic test shape (round-trip identity, robust to capture-direction):
       ```ts
       const before = joiner.view.state.selection;
       joiner.view.dispatch({ changes: { from: 5, insert: "X" }, selection: EditorSelection.cursor(6) });
       await Promise.resolve();
       const after = joiner.view.state.selection;
       joiner.undoManager.undo();
       await flushAll(joiner);
       const undone = joiner.view.state.selection;
       expect(undone.main.head).not.toBe(after.main.head); // selection moved, not stuck
       joiner.undoManager.redo();
       await flushAll(joiner);
       expect(joiner.view.state.selection.main.head).toBe(after.main.head); // round-trip identity
       ```

    7. `it("yjs undo extension wired — joiner has Y.UndoManager and no historyField", ...)`
       - Build joiner via `makeJoinerPeer("joiner")`.
       - Assert `expect(joiner.undoManager).toBeDefined();` AND
         `expect(joiner.undoManager.constructor.name).toBe("UndoManager");`
         (or assert it is an instance of `Y.UndoManager` — import `* as Y` and
         use `expect(joiner.undoManager).toBeInstanceOf(Y.UndoManager);`).
       - Assert `expect(joiner.view.state.field(historyField, false)).toBeUndefined();`
       - Together these prove criterion #5: `createYjsUndoExtension` is the
         ONLY undo mechanism installed on the joiner. If `historyField` were
         present (history() leaked through), CM history would also fire on
         Cmd-z. If `undoManager` were undefined, the joiner would have no
         undo at all.
       - Teardown the joiner.

        Each `it` block must call `teardown(peer)` for every peer it created
    (use `afterEach` or inline). No leaked Y.Docs across tests.

    4-space indent, 100-char lines, semicolons, trailing commas.
    `bunx biome format --write src/lib/collab/joiner-view.test.ts` after writing.
  </action>
  <verify>
    <automated>bun run test:run src/lib/collab/joiner-view.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `src/lib/collab/joiner-view.test.ts`
    - `grep -c "^    it(" src/lib/collab/joiner-view.test.ts` returns at least 7 (seven it blocks covering JOINER-01/-03/-05 + criteria #5/#6/#7/#8)
    - All seven test-name substrings present (each grep returns at least 1):
      - `grep -n "no history field" src/lib/collab/joiner-view.test.ts`
      - `grep -n "cmd-z post-connect" src/lib/collab/joiner-view.test.ts`
      - `grep -n "no annotation duplication" src/lib/collab/joiner-view.test.ts`
      - `grep -n "undo own edits only" src/lib/collab/joiner-view.test.ts`
      - `grep -n "owner history excludes remote text" src/lib/collab/joiner-view.test.ts`
      - `grep -n "selection restored" src/lib/collab/joiner-view.test.ts`
      - `grep -n "yjs undo extension wired" src/lib/collab/joiner-view.test.ts`
    - `grep -nE "makeOwnerPeer\(|history\(\)" src/lib/collab/joiner-view.test.ts` returns at least 1 match (criterion #7 test uses owner peer with CM history — prevents vacuous pass)
    - `grep -n "makeJoinerPeer" src/lib/collab/joiner-view.test.ts` returns at least 4 matches (used in multiple tests)
    - `grep -n "flushAll(" src/lib/collab/joiner-view.test.ts` returns at least 5 matches (flushAll is the ONLY flush primitive)
    - `grep -n "teardown(" src/lib/collab/joiner-view.test.ts` returns at least 6 matches (no leaked peers)
    - `grep -nE "import .*historyField.* from \"@codemirror/commands\"" src/lib/collab/joiner-view.test.ts` returns 1 match
    - `bun run test:run src/lib/collab/joiner-view.test.ts` exits 0 (all tests green)
    - Each individual test passes:
      - `bun run test:run src/lib/collab/joiner-view.test.ts -t "no history field"` exits 0
      - `bun run test:run src/lib/collab/joiner-view.test.ts -t "cmd-z post-connect"` exits 0
      - `bun run test:run src/lib/collab/joiner-view.test.ts -t "no annotation duplication"` exits 0
      - `bun run test:run src/lib/collab/joiner-view.test.ts -t "undo own edits only"` exits 0
      - `bun run test:run src/lib/collab/joiner-view.test.ts -t "owner history excludes remote text"` exits 0
      - `bun run test:run src/lib/collab/joiner-view.test.ts -t "selection restored"` exits 0
      - `bun run test:run src/lib/collab/joiner-view.test.ts -t "yjs undo extension wired"` exits 0
    - `bun run test:run src/lib/collab/` exits 0 (full collab suite green; no Phase 1 regressions)
    - `bunx biome lint src/lib/collab/joiner-view.test.ts` exits 0
  </acceptance_criteria>
  <done>
    All seven Phase 02 success criteria are verified by automated tests.
    Phase 02 is ready for `/gsd-verify-work`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test suite ↔ production code | Tests must NOT modify production source; only assert observable behavior |
| Two-peer test isolation | Each `it` must teardown both peers; leaked Y.Docs cause flaky tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | Tampering | test isolation | mitigate | `teardown(peer)` enforced by acceptance grep ≥ 6 matches |
| T-02-14 | Denial of Service | flushAll iteration cap | accept | Phase 1 sets FLUSH_ALL_MAX_ITERATIONS=20 with loud throw; tests fail loudly on convergence stalls |
</threat_model>

<verification>
- All 7 named tests green
- `bun run test:run src/lib/collab/` is fully green (Phase 1 baseline + Phase 2 additions)
- 02-VALIDATION.md test-name map fully populated
</verification>

<success_criteria>
- Plan 02-05 is the verification gate; on green, Phase 02 success criteria #1–#8 are demonstrably met
- File is the canonical regression suite for joiner-view behavior
</success_criteria>

<output>
After completion, create `.planning/phases/02-joiner-view-hardening/02-05-SUMMARY.md`. Update `02-VALIDATION.md` Per-Task Verification Map with the seven test-name → task-ID rows (task IDs: 02-05-01 covers all seven assertions).
</output>
