---
phase: 02
plan: 04
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/lib/collab/yjsBinding.ts
  - src/lib/collab/yjsAnnotations.ts
  - src/lib/collab/yjsAnnotations.test.ts
autonomous: true
requirements: [JOINER-02, JOINER-05]
nyquist_compliant: true
tags: [collab, yjs, codemirror, undo, annotation-sync, race-fix]
must_haves:
  truths:
    - "yjsBinding.ts:62 remote-text dispatch includes Transaction.addToHistory.of(false)"
    - "_syncInitialToYjs is structurally re-entrance-guarded: a second invocation on the same plugin instance is a no-op"
    - "deepObserver does not double-dispatch addAnnotation for keys that the initial pull is responsible for"
    - "Owner annotation count after joiner connect is unchanged (no double-publish on owner side)"
  artifacts:
    - path: "src/lib/collab/yjsBinding.ts"
      provides: "Remote-text dispatch with Transaction.addToHistory.of(false)"
      contains: "Transaction.addToHistory.of(false)"
    - path: "src/lib/collab/yjsAnnotations.ts"
      provides: "_initialSyncDone re-entrance flag + race fix for 2x annotation duplication"
      contains: "_initialSyncDone"
    - path: "src/lib/collab/yjsAnnotations.test.ts"
      provides: "re-entrance guard test for _syncInitialToYjs"
      contains: "re-entrance guard"
  key_links:
    - from: "src/lib/collab/yjsBinding.ts"
      to: "@codemirror/state Transaction"
      via: "import"
      pattern: "import.*Transaction.*from \"@codemirror/state\""
---

<objective>
Three tightly-coupled fixes in the collab/Yjs sync layer that together
deliver JOINER-02, JOINER-05, and criterion #7:

1. **`yjsBinding.ts:62`**: add `Transaction.addToHistory.of(false)` to the
   remote-text dispatch annotations array. The only currently-missing site
   in the four-guard origin discipline. Without this, owner's Cmd-z replays
   the joiner's incoming text edits.
2. **`yjsAnnotations.ts:234-265`**: add a structural `_initialSyncDone`
   re-entrance flag on `_syncInitialToYjs` so a second call within the same
   plugin lifetime is a no-op (defense in depth alongside the existing
   per-key `has()` check).
3. **`yjsAnnotations.ts` 2x annotation race**: per RESEARCH §4 recommendation
   (b), attach the `deepObserver` ONLY AFTER the initial pull microtask
   completes, so the deepObserver cannot race-double-dispatch
   `addAnnotation` for keys the initial pull is responsible for.

Purpose: these are the surgical changes for owner-side history hygiene
(criterion #7), joiner-side initial sync correctness (JOINER-02), and the 2x
annotation duplication root cause (JOINER-05).

Output: Three files modified; one new test case in the existing
`yjsAnnotations.test.ts`. End-to-end assertions land in Plan 02-05.
</objective>

<execution_context>
@/Users/bryanhu/Developer/current/Quillium/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bryanhu/Developer/current/Quillium/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-joiner-view-hardening/02-CONTEXT.md
@.planning/phases/02-joiner-view-hardening/02-RESEARCH.md
@.planning/phases/02-joiner-view-hardening/02-VALIDATION.md
@.planning/phases/01-test-harness-invariants/01-SUMMARY.md
@src/lib/collab/yjsBinding.ts
@src/lib/collab/yjsAnnotations.ts
@src/lib/collab/yjsAnnotations.test.ts

<interfaces>
<!-- yjsBinding.ts:62 — current shape (RESEARCH §9): -->
```ts
this.view.dispatch({
    changes,
    annotations: [yjsAnnotation.of(true)],
});
```

<!-- yjsBinding.ts current import (RESEARCH §9): -->
```ts
// only Annotation imported on line 14 — Transaction NOT yet imported
import { Annotation } from "@codemirror/state";
```

<!-- yjsAnnotations.ts:234-265 — current _syncInitialToYjs shape (RESEARCH §4): -->
```ts
private _syncInitialToYjs() {
    const ydoc = scopeYtext.doc;
    if (!ydoc) return;
    const annotations = this.view.state.field(annotationField);
    const annotationIds = Object.keys(annotations);
    if (annotationIds.length === 0) return;
    queueMicrotask(() => {
        if (this.destroyed) return;
        ydoc.transact(() => { ... }, "local");
    });
}
```

<!-- _syncInitialFromYjs at line 267 also fires in constructor; runs in microtask -->
<!-- deepObserver attached in constructor (line ~119) -->
<!-- The 2x bug: deepObserver fires for remote updates that arrive between mount and the initial-pull microtask -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Transaction.addToHistory.of(false) to yjsBinding.ts remote-text dispatch</name>
  <files>src/lib/collab/yjsBinding.ts</files>
  <read_first>
    - src/lib/collab/yjsBinding.ts (FULL file — confirm exact line of the remote-text dispatch ~L60-65, confirm `yjsAnnotation` is the local annotation name, confirm the existing import on L14 imports `Annotation` only)
    - .planning/phases/02-joiner-view-hardening/02-CONTEXT.md (§Undo architecture — owner side; this is the "known-missing site")
    - .planning/phases/02-joiner-view-hardening/02-RESEARCH.md (§5 inventory table confirms this is the ONLY missing site; §9 shows the exact before/after diff)
  </read_first>
  <action>
    1. Locate the existing import on or near line 14:
       ```ts
       import { Annotation } from "@codemirror/state";
       ```
       REPLACE with:
       ```ts
       import { Annotation, Transaction } from "@codemirror/state";
       ```
       (single import line, alphabetical or original-order — match existing
       file convention; do NOT create a second import statement.)

    2. Locate the remote-text dispatch around L60-65 inside the Y.Text observer
       (the dispatch that has `annotations: [yjsAnnotation.of(true)]`).
       REPLACE the annotations array with:
       ```ts
       annotations: [yjsAnnotation.of(true), Transaction.addToHistory.of(false)],
       ```
       PRESERVE the surrounding `this.view.dispatch({ changes, annotations: [...] })`
       structure exactly — do NOT introduce a new dispatch call or re-order the
       `changes` field.

    3. Add a one-line comment immediately above the dispatch:
       ```ts
       // Owner-side invariant: remote-text dispatches must NOT enter CM history
       // (otherwise owner's Cmd-z replays joiner's edits). See JOINER criterion #7.
       ```

    4. 4-space indent, 100-char lines. `bunx biome format --write src/lib/collab/yjsBinding.ts`.
  </action>
  <verify>
    <automated>bun run test:run src/lib/collab/yjsBinding.test.ts src/lib/collab/yjsBinding.convergence.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "import \{ Annotation, Transaction \} from \"@codemirror/state\"" src/lib/collab/yjsBinding.ts` returns exactly 1 match (single combined import)
    - `grep -c "from \"@codemirror/state\"" src/lib/collab/yjsBinding.ts` returns exactly 1 (no duplicate import)
    - `grep -n "Transaction.addToHistory.of(false)" src/lib/collab/yjsBinding.ts` returns exactly 1 match
    - `grep -n "yjsAnnotation.of(true), Transaction.addToHistory.of(false)" src/lib/collab/yjsBinding.ts` returns exactly 1 match (correct array shape)
    - `grep -n "Owner-side invariant: remote-text dispatches must NOT enter CM history" src/lib/collab/yjsBinding.ts` returns 1 match
    - `bun run test:run src/lib/collab/yjsBinding.test.ts src/lib/collab/yjsBinding.convergence.test.ts` exits 0
    - `bunx biome lint src/lib/collab/yjsBinding.ts` exits 0
  </acceptance_criteria>
  <done>The four-guard origin discipline is now complete; criterion #7 mechanism in place.</done>
</task>

<task type="auto">
  <name>Task 2: Add _initialSyncDone re-entrance guard + fix 2x annotation race in yjsAnnotations.ts</name>
  <files>src/lib/collab/yjsAnnotations.ts</files>
  <read_first>
    - src/lib/collab/yjsAnnotations.ts (FULL file with focus on L100-300 — confirm class member style, confirm constructor at ~L100-125 calls `_syncInitialToYjs` and `_syncInitialFromYjs`, confirm deepObserver attach site, confirm `_syncInitialFromYjs` shape at L267-299)
    - .planning/phases/02-joiner-view-hardening/02-RESEARCH.md (§4 — both the re-entrance fix AND the 2x race fix recommendation (b): "attach the deepObserver only AFTER the initial pull is complete")
    - .planning/phases/02-joiner-view-hardening/02-CONTEXT.md (§Annotation duplication on connect — confirms the user's locked decision is to fix re-entrance + duplication)
  </read_first>
  <action>
    Two related fixes in this file:

    **Fix A — re-entrance guard on `_syncInitialToYjs` (JOINER-02):**

    1. Add a private instance field to the class (near other `private` fields):
       ```ts
       private _initialSyncDone = false;
       ```

    2. As the FIRST two lines of `_syncInitialToYjs` (before the existing
       `const ydoc = scopeYtext.doc;` line at ~L235), insert:
       ```ts
       if (this._initialSyncDone) return;
       this._initialSyncDone = true;
       ```

    3. PRESERVE the per-key `if (scopeAnnotations.has(yjsId)) continue;` check
       inside the transact block (defense in depth — RESEARCH §4 explicitly
       says "Combine with the per-key has() check (keep both)").

    **Fix B — race fix for 2x annotation duplication (JOINER-05):**

    Apply RESEARCH §4 recommendation (b): attach the `deepObserver` only AFTER
    the initial pull microtask completes.

    1. Locate the deepObserver attachment site in the constructor (currently
       around L119, BEFORE the calls to `_syncInitialToYjs` /
       `_syncInitialFromYjs` complete). Capture the existing observer function
       reference into a class field if not already (e.g., `this._deepObserver = (events) => { ... }`) — this is required so we can `unobserveDeep` it later for the existing lifecycle and so we can defer attach.

    2. REMOVE the synchronous `scopeAnnotations.observeDeep(this._deepObserver)` call from the constructor.

    3. ADD a private method `_attachDeepObserverAfterInitialPull()`:
       ```ts
       private _attachDeepObserverAfterInitialPull(): void {
           // RESEARCH §4 fix (b): attach deepObserver only AFTER the initial
           // pull microtask completes; otherwise a remote update arriving
           // between mount and the pull microtask races _syncInitialFromYjs
           // and produces 2x annotations on the joiner. See JOINER-05.
           queueMicrotask(() => {
               if (this.destroyed) return;
               scopeAnnotations.observeDeep(this._deepObserver);
           });
       }
       ```
       (Use the actual variable names present in the file — `scopeAnnotations`
       is the Y.Map, `this._deepObserver` is the stored function ref. If the
       file uses a different name for the Y.Map, USE THE FILE'S NAME.)

    4. CALL `this._attachDeepObserverAfterInitialPull()` in the constructor
       immediately after `_syncInitialFromYjs()` is invoked (so the attach is
       enqueued AFTER the initial pull's microtask).

    5. UPDATE `destroy()` to call `unobserveDeep` only if the observer was
       actually attached. Easiest: keep the existing `unobserveDeep(this._deepObserver)` call — Y.Map's `unobserveDeep` is a no-op for not-attached observers. Add a one-line comment confirming this is safe.

    6. PRESERVE the existing `_syncInitialFromYjs` body and its `addAnnotation`
       dispatch with `[yjsAnnotationSync.of(true), Transaction.addToHistory.of(false)]` annotations — do NOT modify the dispatch.

    4-space indent, 100-char lines. `bunx biome format --write src/lib/collab/yjsAnnotations.ts`.
  </action>
  <verify>
    <automated>bun run test:run src/lib/collab/yjsAnnotations.test.ts src/lib/collab/annotation-sync.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "private _initialSyncDone = false;" src/lib/collab/yjsAnnotations.ts` returns exactly 1 match
    - `grep -nE "if \(this\._initialSyncDone\) return;" src/lib/collab/yjsAnnotations.ts` returns exactly 1 match
    - `grep -nE "this\._initialSyncDone = true;" src/lib/collab/yjsAnnotations.ts` returns exactly 1 match
    - The two lines above appear inside `_syncInitialToYjs` BEFORE the existing `const ydoc = scopeYtext.doc;` line — verify with `awk '/_syncInitialToYjs\(\)/,/scopeYtext\.doc/' src/lib/collab/yjsAnnotations.ts | grep -c "_initialSyncDone"` returns 2
    - `grep -n "_attachDeepObserverAfterInitialPull" src/lib/collab/yjsAnnotations.ts` returns at least 2 matches (definition + call)
    - `grep -n "queueMicrotask" src/lib/collab/yjsAnnotations.ts` returns at least 2 matches (existing in `_syncInitialToYjs` + new in `_attachDeepObserverAfterInitialPull`)
    - `grep -n "RESEARCH §4 fix (b)" src/lib/collab/yjsAnnotations.ts` returns 1 match (intent documented)
    - `grep -n "scopeAnnotations.has(yjsId)" src/lib/collab/yjsAnnotations.ts` returns at least 1 match (defense-in-depth check preserved)
    - The deepObserver is NOT attached synchronously in the constructor: `awk '/constructor/,/^    \}/' src/lib/collab/yjsAnnotations.ts | grep -c "scopeAnnotations.observeDeep"` returns 0 (the only observeDeep call now lives inside `_attachDeepObserverAfterInitialPull`'s queueMicrotask)
    - `grep -c "scopeAnnotations.observeDeep" src/lib/collab/yjsAnnotations.ts` returns exactly 1 (single attach site, deferred)
    - `bun run test:run src/lib/collab/yjsAnnotations.test.ts src/lib/collab/annotation-sync.test.ts` exits 0
    - `bunx biome lint src/lib/collab/yjsAnnotations.ts` exits 0
  </acceptance_criteria>
  <done>
    Re-entrance guard structural; deepObserver attached only after initial
    pull. JOINER-02 + JOINER-05 mechanisms in place; end-to-end assertions
    land in Plan 02-05.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add re-entrance guard test to yjsAnnotations.test.ts</name>
  <files>src/lib/collab/yjsAnnotations.test.ts</files>
  <read_first>
    - src/lib/collab/yjsAnnotations.test.ts (FULL file — confirm test framework imports, existing test setup pattern, how a fresh sync plugin instance is constructed in tests)
    - src/lib/collab/yjsAnnotations.ts (post-Task-2 state — confirm the `_initialSyncDone` flag is in place)
    - .planning/phases/02-joiner-view-hardening/02-VALIDATION.md (Wave 0 row for JOINER-02 — the test name MUST match the validation map)
  </read_first>
  <action>
    EXTEND `src/lib/collab/yjsAnnotations.test.ts` (do NOT create a new file).
    Add a new `it()` test inside the appropriate `describe()` block (or add a
    new `describe("re-entrance guard")` block if no suitable parent exists).

    Test name MUST be exactly: `"re-entrance guard: _syncInitialToYjs is a no-op on second invocation"`.

    Test body:
    1. Construct a fresh Y.Doc + Y.Text + Y.Map; create CM EditorState with
       `annotationField` populated with at least 2 annotations.
    2. Construct an EditorView with `createAnnotationSyncPlugin(ymap)` (or
       whatever constructor exists). The constructor's
       `_syncInitialToYjs()` will fire once.
    3. `await Promise.resolve()` (drain the queueMicrotask in `_syncInitialToYjs`).
    4. Capture `const initialYMapKeys = Array.from(ymap.keys()).length;` —
       record what the first invocation published.
    5. Reach into the plugin instance to call `_syncInitialToYjs()` again
       (use a typed escape hatch — cast the plugin spec or call the public
       method if exposed; if `_syncInitialToYjs` is private, expose a test-only
       hook via TypeScript private-member access: `(plugin as any)._syncInitialToYjs()`).
    6. `await Promise.resolve()`.
    7. Assert: `expect(Array.from(ymap.keys()).length).toBe(initialYMapKeys);`
       — second call did not republish, did not duplicate, did not throw.
    8. Teardown: `view.destroy(); ydoc.destroy();`.

    Use 4-space indent, 100-char lines. Match the existing import + setup
    style of the file exactly.
  </action>
  <verify>
    <automated>bun run test:run src/lib/collab/yjsAnnotations.test.ts -t "re-entrance guard"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "re-entrance guard: _syncInitialToYjs is a no-op on second invocation" src/lib/collab/yjsAnnotations.test.ts` returns exactly 1 match
    - `bun run test:run src/lib/collab/yjsAnnotations.test.ts -t "re-entrance guard"` exits 0
    - `bun run test:run src/lib/collab/yjsAnnotations.test.ts` exits 0 (no regression in the rest of the file)
    - `bunx biome lint src/lib/collab/yjsAnnotations.test.ts` exits 0
  </acceptance_criteria>
  <done>JOINER-02 has a focused regression test that will fail if the re-entrance flag is removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Owner CM history ↔ remote Y.Text updates | Without `addToHistory.of(false)`, owner's Cmd-z can replay joiner edits — confused-deputy scenario for undo intent |
| Plugin lifecycle ↔ deepObserver attachment timing | A race between initial pull and observer firing duplicates annotations on joiner |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Tampering | owner CM history | mitigate | `Transaction.addToHistory.of(false)` excludes remote-text dispatches from history; verified by Plan 02-05's "owner history excludes remote text" test |
| T-02-10 | Tampering | annotation Y.Map seed | mitigate | Structural `_initialSyncDone` flag prevents double-publish; per-key `has()` check is defense-in-depth |
| T-02-11 | Denial of Service | 2x annotation rebuild | mitigate | deepObserver attach deferred until after initial pull; race window closed |
| T-02-12 | Repudiation | who originated an undone op | accept | Cross-peer undo semantics are documented in CONTEXT.md (Google Docs model); this is intentional behavior |
</threat_model>

<verification>
- All acceptance grep checks pass for all three tasks
- `bun run test:run src/lib/collab/` green
- The four-guard origin discipline (per yjsAnnotations.ts top-of-file
  invariants from Phase 1) is now COMPLETE — all four sites verified
- The 2x annotation race is structurally closed
</verification>

<success_criteria>
- yjsBinding.ts:62 dispatch carries `Transaction.addToHistory.of(false)`
- `_syncInitialToYjs` cannot run twice for the same plugin instance
- deepObserver does not race the initial pull
- Re-entrance regression test in place
</success_criteria>

<output>
After completion, create `.planning/phases/02-joiner-view-hardening/02-04-SUMMARY.md`.
</output>
