---
phase: 08.5a-crdt-data-shape
plan: 01
subsystem: testing
tags: [yjs, collab, crdt, vitest, test-harness, wave-0]

# Dependency graph
requires:
  - phase: 07.5-yjs-migration
    provides: createYjsBinding and createAnnotationSyncPlugin that the harness wires together
  - phase: 08-annotation-sync
    provides: current Y.Map<YjsAnnotation> shape the harness stays compatible with
provides:
  - Two-peer Y.Doc test fixture (makePeer, connect, teardown, Peer) for convergence suites
  - 19 `it.todo` placeholders across 8 test files, each addressable by the exact `-t "<pattern>"` string in 08.5a-VALIDATION.md
  - Per-layer test file skeletons (subtree-binding, undo-manager, annotation-tree, version-coordination, subtree-convergence, convergence-edgecases, thread-append, recursive-mount)
affects: [08.5a-02, 08.5b-01, 08.5b-02, 08.5c-01, 08.5c-02]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Shared two-peer harness extracted from duplicated convergence-test helpers"
    - "it.todo as concrete wiring targets — each downstream feature task rewrites a named todo into a real `it(...)` with assertions"
    - "describe/todo strings lifted verbatim from VALIDATION.md Per-Task Verification Map so `-t` flags hit exactly one test"

key-files:
  created:
    - src/lib/collab/test-helpers/twoPeerHarness.ts
    - src/lib/collab/subtree-binding.test.ts
    - src/lib/collab/undo-manager.test.ts
    - src/lib/collab/annotation-tree.test.ts
    - src/lib/collab/version-coordination.test.ts
    - src/lib/collab/subtree-convergence.test.ts
    - src/lib/collab/convergence-edgecases.test.ts
    - src/lib/collab/thread-append.test.ts
    - src/lib/collab/recursive-mount.test.ts
  modified: []

key-decisions:
  - "Use `it.todo` rather than `it.skip` so Vitest reports todos distinctly and the suite stays green (exit 0) while surfacing outstanding work"
  - "Keep Peer.ymap typed as `Y.Map<unknown>` with a `Y.Map<never>` cast at the createAnnotationSyncPlugin boundary so the harness survives the shape rewrite in Plan 8.5a-02"
  - "Do NOT import twoPeerHarness into Wave 0 scaffolds yet — empty todo bodies need no harness; wiring happens when downstream plans replace `it.todo` with real tests"

patterns-established:
  - "Wave 0 scaffolding convention: every feature task claims an `<automated>` verify by rewriting a pre-created `it.todo` with a matching describe/pattern pair"
  - "Harness-first testing for convergence suites: one canonical makePeer/connect/teardown that downstream tests import rather than re-deriving"

requirements-completed: [SYNC-05]

# Metrics
duration: 8min
completed: 2026-04-18
---

# Phase 08.5a Plan 01: Wave 0 Test Scaffolding Summary

**Two-peer Y.Doc harness plus 19 it.todo placeholders across 8 layered test files, giving every downstream Phase 8.5 task a concrete `-t "<pattern>"` verify target.**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-04-18T21:45:00Z
- **Completed:** 2026-04-18T21:53:17Z
- **Tasks:** 2
- **Files created:** 9 (1 harness + 8 test scaffolds)
- **Files modified:** 0 (production code untouched)

## Accomplishments

- Extracted the duplicated `makePeer` / `connect` / `teardown` triple from `yjsBinding.convergence.test.ts` and `yjsAnnotations.convergence.test.ts` into a single shared fixture at `src/lib/collab/test-helpers/twoPeerHarness.ts`.
- Created 8 per-layer test files covering the Nyquist dimensions called out in 08.5a-VALIDATION.md (subtree binding, undo manager, recursive tree shape, version coordination, 2-peer convergence, yjs#642 edgecases, thread append, recursive mount).
- Wired 19 `it.todo` entries whose describe + pattern strings match the exact `-t "<pattern>"` commands in the Per-Task Verification Map, so downstream plans (`8.5a-02`, `8.5b-01`, `8.5b-02`, `8.5c-01`, `8.5c-02`) can claim per-task verify by rewriting a single todo.

## Task Commits

1. **Task 1: Create two-peer test harness (twoPeerHarness.ts)** — `06cad2b` (test)
2. **Task 2: Create Wave 0 test scaffolds (8 files)** — `6d447bb` (test)

## Files Created/Modified

- `src/lib/collab/test-helpers/twoPeerHarness.ts` — Shared two-peer Y.Doc fixture; exports `makePeer`, `connect`, `teardown`, `Peer`.
- `src/lib/collab/subtree-binding.test.ts` — Subtree Y.Text ↔ CodeMirror binding fidelity (2 todos; wired in Plan 8.5c-01).
- `src/lib/collab/undo-manager.test.ts` — Y.UndoManager behavior matrix (6 todos; wired in Plans 8.5b-02 and 05).
- `src/lib/collab/annotation-tree.test.ts` — Recursive Y.Map shape + observeDeep (2 todos; wired in Plans 8.5a-02 and 8.5b-01).
- `src/lib/collab/version-coordination.test.ts` — `versions: Y.Map` + D-99 local `activeVersionIndex` (2 todos; wired in Plans 8.5b-01 and 8.5c-01).
- `src/lib/collab/subtree-convergence.test.ts` — 2-peer concurrent edits inside v0 (2 todos; wired in Plan 8.5c-02).
- `src/lib/collab/convergence-edgecases.test.ts` — yjs#642 reproduction + mitigation (2 todos; wired in Plan 8.5c-02).
- `src/lib/collab/thread-append.test.ts` — Y.Array append + sort-by-ts (2 todos; wired in Plan 8.5b-01).
- `src/lib/collab/recursive-mount.test.ts` — Nested-in-nested subtree binding (1 todo; wired in Plan 8.5c-01).

## Decisions Made

Followed the plan's action blocks verbatim. The file contents were pre-specified; no design choices were made during execution.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

One transient lint-command side effect: `bun run lint src/lib/collab/` runs Biome with `--write`, which on the first invocation auto-fixed pre-existing style issues in three unrelated collab files (`annotationSchema.ts`, `awareness.ts`, `index.ts`). Those fixes were out-of-scope for this plan (pre-existing findings in files owned by later plans), so the working-tree changes were reverted with `git checkout -- …` before committing Task 2. Only the 8 new test files were staged. The pre-existing lint errors remain unchanged in the repo and are tracked to the appropriate owning plans; they are not regressions introduced by this plan.

The 8 new test files themselves lint cleanly (`bun run lint src/lib/collab/test-helpers/ src/lib/collab/{subtree-binding,undo-manager,annotation-tree,version-coordination,subtree-convergence,convergence-edgecases,thread-append,recursive-mount}.test.ts` exits 0 with "No fixes applied").

## Verification

| Check | Result |
|-------|--------|
| File `src/lib/collab/test-helpers/twoPeerHarness.ts` exists | Yes |
| Exports `makePeer`, `connect`, `teardown`, `Peer` | Yes (`grep -E "^export (function\|interface)"` returns all four) |
| Harness imports `createYjsBinding` and `createAnnotationSyncPlugin` | Yes |
| 8 new test files exist in `src/lib/collab/` | Yes |
| Total `it.todo` entries across new scaffolds | 19 (2+6+2+2+2+2+2+1) |
| Describe strings match VALIDATION.md Per-Task Verification Map | Yes (all 8 verified) |
| No `expect(...)` or bare `it(` in new scaffolds | Confirmed (grep returns no matches) |
| `bun run test:run src/lib/collab/` exit code | 0 (155 passed · 19 todo · 0 new failures) |
| Lint on just the new files | Clean (Biome reports "No fixes applied") |

## Downstream Consumption Map

Each entry below is the exact `-t "<pattern>"` a downstream task will target when it wires its assertion into the matching `it.todo`:

| Plan | File | Pattern |
|------|------|---------|
| 8.5a-02 | annotation-tree.test.ts | `version propagation` |
| 8.5b-01 | annotation-tree.test.ts | `observeDeep fires for descendant Y.Text changes` |
| 8.5b-01 | version-coordination.test.ts | `concurrent edits into independent versions converge` |
| 8.5b-01 | thread-append.test.ts | `concurrent append` |
| 8.5b-01 | thread-append.test.ts | `sort by ts on read` |
| 8.5b-02 | undo-manager.test.ts | `addToScope captures new type` |
| 8.5b-02 | undo-manager.test.ts | `remote origin ignored` |
| 8.5b-02 | undo-manager.test.ts | `remote updates not captured` |
| 8.5b-02 | undo-manager.test.ts | `auto-nav event emitted` |
| 8.5c-01 | subtree-binding.test.ts | `CM typing writes into subtree Y.Text` |
| 8.5c-01 | subtree-binding.test.ts | `remote insert into subtree Y.Text updates CM` |
| 8.5c-01 | version-coordination.test.ts | `activeVersionIndex is local metadata only` |
| 8.5c-01 | recursive-mount.test.ts | `nested-in-nested: level 2 subtree binding fires` |
| 8.5c-02 | subtree-convergence.test.ts | `concurrent edit inside v0` |
| 8.5c-02 | subtree-convergence.test.ts | `concurrent inserts at different positions` |
| 8.5c-02 | convergence-edgecases.test.ts | `version churn convergence` |
| 8.5c-02 | convergence-edgecases.test.ts | `rapid set+delete under captureTimeout boundary` |
| 05 (cross-editor undo) | undo-manager.test.ts | `chronological across scopes` |
| 05 | undo-manager.test.ts | `nested local undo` |

## User Setup Required

None - Wave 0 scaffolding is a pure test-code addition; no external services or env vars.

## Next Phase Readiness

- Plan 8.5a-02 can now run `bun run test:run src/lib/collab/annotation-tree.test.ts -t "version propagation"` and see a pre-existing todo slot to rewrite as a real assertion.
- The harness is importable from `./test-helpers/twoPeerHarness` (relative) by any new convergence test that needs two connected Y.Docs.
- All Phase 8.5 Wave 0 requirements from 08.5a-VALIDATION.md are satisfied except the E2E spec `tests/e2e/undoAutoNavigate.pw.ts`, which is out of scope for this plan (a separate plan owns E2E scaffolding).

## Self-Check: PASSED

All 9 claimed files exist on disk; both task commits (`06cad2b`, `6d447bb`) are present in `git log`.

---
*Phase: 08.5a-crdt-data-shape*
*Plan: 01*
*Completed: 2026-04-18*
