---
phase: 12
plan: 02
title: Unified nested editor integration test
wave: 1
depends_on: [12-01]
requirements: [SYNC-05]
files_modified:
  - src/lib/collab/annotation-sync.test.ts
autonomous: true
---

# Plan 12-02: Unified nested editor integration test

<objective>
Prove the inline nested editor works identically with collab on vs off: create revision, type in inline editor, switch version, add/delete version.
</objective>

<tasks>
<task id="1">
<title>Add comprehensive nested editor sync test</title>
<read_first>
- src/lib/collab/annotation-sync.test.ts (existing patterns)
</read_first>
<action>
Add a new describe block "nested editor sync (Phase 12)" with tests that prove:

1. Create revision → joiner sees it
2. Type in inline editor (multiple characters) → joiner sees each change
3. Switch version → joiner's active version updates
4. Add version → joiner gets new version
5. Delete version → joiner reflects deletion

These overlap with existing tests but specifically exercise the nested editor flow (nestedEditorEdit annotation).
</action>
<acceptance_criteria>
- `bun run test:run src/lib/collab/annotation-sync.test.ts` passes all tests
- Test file contains "nested editor sync (Phase 12)" describe block
</acceptance_criteria>
</task>
</tasks>

<verification>
```bash
bun run test:run src/lib/collab/annotation-sync.test.ts
bun run test:run src/lib/collab/
```
</verification>

<must_haves>
- Tests prove nested editor typing syncs (not just metadata operations)
- Tests cover version switching from within nested editor context
</must_haves>
