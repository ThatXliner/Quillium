---
phase: 10-strip-broken-collab-sync-layer
plan: 04
type: execute
wave: 2
depends_on: [01, 02, 03]
files_modified:
  - src/lib/collab/revision-lifecycle.test.ts
  - src/lib/collab/version-coordination.test.ts
  - src/lib/collab/go-live-mid-session.test.ts
  - src/lib/collab/nested-editor-sync.test.ts
  - src/lib/collab/version-switch-live.test.ts
  - src/lib/collab/subtree-binding.test.ts
  - src/lib/collab/subtree-convergence.test.ts
  - src/lib/collab/recursive-mount.test.ts
  - src/lib/collab/convergence-edgecases.test.ts
autonomous: true
requirements: [SYNC-05]
must_haves:
  truths:
    - "Tests coupled to old sync-plugin design are deleted"
    - "Tests that exercise deleted code paths no longer exist"
  artifacts:
    - path: "src/lib/collab/"
      provides: "Test files deleted or emptied for rebuild in Phase 13"
  key_links: []
---

<objective>
Delete all tests that are coupled to the old annotation sync plugin design. These tests exercise code paths that no longer exist after Plans 01-03.

Purpose: The tests for revision-lifecycle, version-coordination, go-live-mid-session, nested-editor-sync, version-switch-live, and subtree-binding were written against the broken sync layer. They will fail or be meaningless after the code they test is deleted. Phase 13 will create new integration tests for the rebuilt sync.

Output: Test files deleted that were coupled to the broken sync layer.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete tests coupled to old sync-plugin design</name>
  <files>
    src/lib/collab/revision-lifecycle.test.ts,
    src/lib/collab/version-coordination.test.ts,
    src/lib/collab/go-live-mid-session.test.ts,
    src/lib/collab/nested-editor-sync.test.ts,
    src/lib/collab/version-switch-live.test.ts,
    src/lib/collab/subtree-binding.test.ts,
    src/lib/collab/subtree-convergence.test.ts,
    src/lib/collab/recursive-mount.test.ts,
    src/lib/collab/convergence-edgecases.test.ts
  </files>
  <read_first>
    - src/lib/collab/revision-lifecycle.test.ts (verify it tests deleted code)
    - src/lib/collab/version-coordination.test.ts (verify it tests deleted code)
    - src/lib/collab/go-live-mid-session.test.ts (verify it tests deleted code)
    - src/lib/collab/nested-editor-sync.test.ts (verify it tests deleted code)
    - src/lib/collab/version-switch-live.test.ts (verify it tests deleted code)
    - src/lib/collab/subtree-binding.test.ts (verify it tests deleted code)
    - src/lib/collab/subtree-convergence.test.ts (verify it tests deleted code)
    - src/lib/collab/recursive-mount.test.ts (verify it tests deleted code)
    - src/lib/collab/convergence-edgecases.test.ts (verify it tests deleted code)
  </read_first>
  <action>
Delete the following test files entirely using `rm`:

1. `src/lib/collab/revision-lifecycle.test.ts` - Tests Phase 9 bug invariants that rely on subtree Y.Text sync
2. `src/lib/collab/version-coordination.test.ts` - Tests activeVersionIndex sync via Y.Map which is now disabled
3. `src/lib/collab/go-live-mid-session.test.ts` - Tests needsCollabModeRebuild which is deleted
4. `src/lib/collab/nested-editor-sync.test.ts` - Tests subtree Y.Text sync with annotations which is disabled
5. `src/lib/collab/version-switch-live.test.ts` - Tests version switch in collab mode which is disabled
6. `src/lib/collab/subtree-binding.test.ts` - Scaffold for subtree binding tests (Wave 0 todos)
7. `src/lib/collab/subtree-convergence.test.ts` - Scaffold for subtree convergence (Wave 0 todos)
8. `src/lib/collab/recursive-mount.test.ts` - Scaffold for recursive mount (Wave 0 todos)
9. `src/lib/collab/convergence-edgecases.test.ts` - Scaffold for edge cases (Wave 0 todos)

Run:
```bash
rm src/lib/collab/revision-lifecycle.test.ts
rm src/lib/collab/version-coordination.test.ts
rm src/lib/collab/go-live-mid-session.test.ts
rm src/lib/collab/nested-editor-sync.test.ts
rm src/lib/collab/version-switch-live.test.ts
rm src/lib/collab/subtree-binding.test.ts
rm src/lib/collab/subtree-convergence.test.ts
rm src/lib/collab/recursive-mount.test.ts
rm src/lib/collab/convergence-edgecases.test.ts
```

These tests will be replaced by Phase 13's dogfooding regression suite.
  </action>
  <verify>
    <automated>! ls src/lib/collab/revision-lifecycle.test.ts 2>/dev/null && ! ls src/lib/collab/version-coordination.test.ts 2>/dev/null && ! ls src/lib/collab/go-live-mid-session.test.ts 2>/dev/null && echo "Old sync tests deleted"</automated>
  </verify>
  <acceptance_criteria>
    - `revision-lifecycle.test.ts` does not exist
    - `version-coordination.test.ts` does not exist
    - `go-live-mid-session.test.ts` does not exist
    - `nested-editor-sync.test.ts` does not exist
    - `version-switch-live.test.ts` does not exist
    - `subtree-binding.test.ts` does not exist
    - `subtree-convergence.test.ts` does not exist
    - `recursive-mount.test.ts` does not exist
    - `convergence-edgecases.test.ts` does not exist
  </acceptance_criteria>
  <done>All tests coupled to old sync-plugin design are deleted</done>
</task>

<task type="auto">
  <name>Task 2: Update twoPeerHarness to not use annotation sync plugin</name>
  <files>src/lib/collab/test-helpers/twoPeerHarness.ts</files>
  <read_first>
    - src/lib/collab/test-helpers/twoPeerHarness.ts (the file being modified)
  </read_first>
  <action>
The twoPeerHarness currently installs `createAnnotationSyncPlugin` in each peer. Since the annotation sync plugin's write path is now disabled, we have two options:

1. Keep the harness as-is (it will still work, just won't sync annotations)
2. Remove the annotation sync plugin from the harness

Choose option 2 - remove the annotation sync plugin from the harness for clarity:

In `src/lib/collab/test-helpers/twoPeerHarness.ts`:

1. Remove the import:
   ```typescript
   // DELETE:
   import { createAnnotationSyncPlugin } from "../yjsAnnotations";
   ```

2. In `makePeer()`, remove the annotation sync plugin from extensions:
   ```typescript
   const state = EditorState.create({
       doc: initialText,
       extensions: [
           annotationField,
           createYjsBinding(ytext),
           // DELETE: createAnnotationSyncPlugin(ytext, ymap as Y.Map<never>, clientId),
       ],
   });
   ```

3. Remove the ymap from the Peer interface and makePeer if it's no longer needed:
   - Check if ymap is used elsewhere in remaining tests
   - If only used by deleted tests, remove it
   
   Keep ymap in the interface for now (Phase 11 may need it), but don't use it in extensions.

4. Add a comment:
   ```typescript
   // Phase 10: Annotation sync plugin removed. Main text sync only.
   // Phase 11 will rebuild annotation sync with unified diff-and-write.
   ```
  </action>
  <verify>
    <automated>! grep -q "createAnnotationSyncPlugin" src/lib/collab/test-helpers/twoPeerHarness.ts && echo "Annotation sync plugin removed from harness"</automated>
  </verify>
  <acceptance_criteria>
    - `createAnnotationSyncPlugin` import removed from twoPeerHarness.ts
    - `createAnnotationSyncPlugin` not called in makePeer()
    - Harness still creates peers with yjsBinding for main text sync
  </acceptance_criteria>
  <done>twoPeerHarness no longer uses annotation sync plugin</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Test deletion only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | N/A | Test deletion only - no production impact |
</threat_model>

<verification>
After both tasks:
```bash
# Verify deleted test files
ls src/lib/collab/*-lifecycle*.test.ts src/lib/collab/*-coordination*.test.ts src/lib/collab/*-mid-session*.test.ts 2>&1 | grep -q "No such file" && echo "PASS: tests deleted"

# Verify harness doesn't use annotation sync
grep "createAnnotationSyncPlugin" src/lib/collab/test-helpers/twoPeerHarness.ts && echo "FAIL" || echo "PASS"
```
</verification>

<success_criteria>
- [ ] 9 test files deleted (revision-lifecycle, version-coordination, go-live-mid-session, nested-editor-sync, version-switch-live, subtree-binding, subtree-convergence, recursive-mount, convergence-edgecases)
- [ ] twoPeerHarness no longer imports createAnnotationSyncPlugin
- [ ] twoPeerHarness no longer installs annotation sync plugin
</success_criteria>

<output>
After completion, create `.planning/phases/10-strip-broken-collab-sync-layer/10-04-SUMMARY.md`
</output>
