---
phase: 10-strip-broken-collab-sync-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/collab/yjsAnnotations.ts
  - src/lib/collab/index.ts
  - src/lib/editor/plugins/annotations/annotationField.ts
autonomous: true
requirements: [SYNC-05]
must_haves:
  truths:
    - "createAnnotationSyncPlugin has no effect-by-effect write handlers"
    - "hasSubtreeForRevision function is deleted"
    - "annotationField Phase 3 runs unconditionally for all revisions"
  artifacts:
    - path: "src/lib/collab/yjsAnnotations.ts"
      provides: "Stripped annotation sync plugin (no write path)"
      contains: "// Phase 10: annotation sync intentionally disabled"
    - path: "src/lib/collab/index.ts"
      provides: "Collab module without hasSubtreeForRevision export"
    - path: "src/lib/editor/plugins/annotations/annotationField.ts"
      provides: "annotationField without hasSubtreeForRevision check"
  key_links:
    - from: "annotationField.ts"
      to: "collab/index.ts"
      via: "import removed"
      pattern: "no hasSubtreeForRevision import"
---

<objective>
Strip the broken annotation sync write path from `createAnnotationSyncPlugin` and remove the `hasSubtreeForRevision` short-circuit from `annotationField` Phase 3.

Purpose: Clean ground for Phase 11's rebuild. The effect-by-effect handlers in `yjsAnnotations.ts` and the Phase 3 short-circuit in `annotationField.ts` are the source of the inconsistency bugs. Removing them entirely (leaving only the observer for future read-back) gives Phase 11 a clean slate to implement a single diff-and-write path.

Output: `yjsAnnotations.ts` with no `update()` write handlers, `annotationField.ts` with Phase 3 running unconditionally, `index.ts` without `hasSubtreeForRevision` export.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gut the createAnnotationSyncPlugin write path</name>
  <files>src/lib/collab/yjsAnnotations.ts</files>
  <read_first>
    - src/lib/collab/yjsAnnotations.ts (the file being modified)
    - src/lib/editor/plugins/annotations/annotationField.ts (to understand effects being removed)
  </read_first>
  <action>
Delete the entire `update(update: ViewUpdate)` method body in the `createAnnotationSyncPlugin` class. Replace it with an early return and a comment:

```typescript
update(_update: ViewUpdate) {
    // Phase 10: annotation sync write path intentionally disabled.
    // Phase 11 will implement a single diff-and-write path.
    return;
}
```

Remove the following imports that are no longer used after gutting the update method:
- `addAnnotation` from annotationField
- `removeAnnotation` from annotationField  
- `updateThread` from annotationField
- `_updateActiveRevisionVersion` from annotationField
- `_addVersionToRevision` from annotationField
- `_deleteVersionFromRevision` from annotationField
- `isAnnotationOfType` from models
- `yjsAnnotationSync` export (mark deprecated but keep for tests)

Keep these imports (still used in constructor/observer):
- `annotationField` (read in observer)
- `codeMirrorToYjsAnnotation`, `yjsAnnotationToCodeMirror`, `AnnotationIdMap` from annotationSchema
- `YjsAnnotationNode`, `MessageObject` from types
- Y.* imports

The observer (`deepObserver`) stays intact for now - it handles read-back from Yjs which will be preserved. Only the write path (update method) is removed.

Also keep `_syncInitialToYjs()` and `_syncInitialFromYjs()` intact - these handle initial state exchange which still works.
  </action>
  <verify>
    <automated>grep -c "effect.is(addAnnotation)" src/lib/collab/yjsAnnotations.ts | grep -q "^0$" && echo "No addAnnotation effect handlers"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/collab/yjsAnnotations.ts` update() method body is `return;` with Phase 10 comment
    - `effect.is(addAnnotation)` appears 0 times in yjsAnnotations.ts
    - `effect.is(removeAnnotation)` appears 0 times in yjsAnnotations.ts
    - `effect.is(updateThread)` appears 0 times in yjsAnnotations.ts
    - `effect.is(_updateActiveRevisionVersion)` appears 0 times in yjsAnnotations.ts
    - `effect.is(_addVersionToRevision)` appears 0 times in yjsAnnotations.ts
    - `effect.is(_deleteVersionFromRevision)` appears 0 times in yjsAnnotations.ts
  </acceptance_criteria>
  <done>createAnnotationSyncPlugin has no effect-by-effect write handlers in update()</done>
</task>

<task type="auto">
  <name>Task 2: Delete hasSubtreeForRevision and remove Phase 3 short-circuit</name>
  <files>src/lib/collab/index.ts, src/lib/editor/plugins/annotations/annotationField.ts</files>
  <read_first>
    - src/lib/collab/index.ts (contains hasSubtreeForRevision)
    - src/lib/editor/plugins/annotations/annotationField.ts (imports and uses hasSubtreeForRevision)
  </read_first>
  <action>
In `src/lib/collab/index.ts`:
1. Delete the `hasSubtreeForRevision` function entirely (lines ~357-369)
2. Remove it from exports if explicitly exported

In `src/lib/editor/plugins/annotations/annotationField.ts`:
1. Delete the import: `import { hasSubtreeForRevision } from "$lib/collab";`
2. In the `pushDocToVersionState` function, delete the short-circuit check:
   ```typescript
   // DELETE THIS BLOCK (around line 600-601):
   // Plan 8.5c-01: Skip Phase 3 for revisions with Yjs subtrees — the
   // subtree Y.Text is the source of truth, not the parent doc slice.
   if (hasSubtreeForRevision(x.id)) return x;
   ```
   
After deletion, Phase 3 will run unconditionally for all revisions, which is the correct behavior for local-only mode and the clean starting point for Phase 11's unified sync.
  </action>
  <verify>
    <automated>! grep -q "hasSubtreeForRevision" src/lib/collab/index.ts && ! grep -q "hasSubtreeForRevision" src/lib/editor/plugins/annotations/annotationField.ts && echo "hasSubtreeForRevision deleted"</automated>
  </verify>
  <acceptance_criteria>
    - `hasSubtreeForRevision` does not appear in `src/lib/collab/index.ts`
    - `hasSubtreeForRevision` does not appear in `src/lib/editor/plugins/annotations/annotationField.ts`
    - No import from `$lib/collab` exists in annotationField.ts
    - `pushDocToVersionState` has no early return based on collab state
  </acceptance_criteria>
  <done>hasSubtreeForRevision deleted, Phase 3 runs unconditionally</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan only deletes code; no new trust boundaries introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | N/A | Code deletion only - reduces attack surface |
</threat_model>

<verification>
After both tasks:
```bash
# Verify no hasSubtreeForRevision anywhere in src/
grep -r "hasSubtreeForRevision" src/ && echo "FAIL: still found" || echo "PASS: deleted"

# Verify update() is neutered
grep -A5 "update(_update: ViewUpdate)" src/lib/collab/yjsAnnotations.ts | grep -q "return;" && echo "PASS: update neutered"
```
</verification>

<success_criteria>
- [ ] `createAnnotationSyncPlugin.update()` body is `return;` with Phase 10 comment
- [ ] No effect handlers for addAnnotation/removeAnnotation/updateThread/_updateActiveRevisionVersion/_addVersionToRevision/_deleteVersionFromRevision
- [ ] `hasSubtreeForRevision` function deleted from index.ts
- [ ] Import of hasSubtreeForRevision removed from annotationField.ts
- [ ] Phase 3 short-circuit removed - pushDocToVersionState runs for all revisions
</success_criteria>

<output>
After completion, create `.planning/phases/10-strip-broken-collab-sync-layer/10-01-SUMMARY.md`
</output>
