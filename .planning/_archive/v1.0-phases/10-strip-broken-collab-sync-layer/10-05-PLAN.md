---
phase: 10-strip-broken-collab-sync-layer
plan: 05
type: execute
wave: 3
depends_on: [01, 02, 03, 04]
files_modified:
  - src/lib/collab/yjsAnnotations.test.ts
  - src/lib/collab/yjsAnnotations.convergence.test.ts
autonomous: true
requirements: [SYNC-05]
must_haves:
  truths:
    - "Typecheck passes"
    - "Biome passes"
    - "Remaining tests pass"
    - "Main text sync between two peers works"
  artifacts:
    - path: "src/lib/collab/yjsAnnotations.test.ts"
      provides: "Updated tests for stripped sync plugin"
    - path: "src/lib/collab/yjsAnnotations.convergence.test.ts"
      provides: "Updated convergence tests or deletions"
  key_links: []
---

<objective>
Fix remaining tests, run typecheck, biome, and verify main text sync still works via twoPeerHarness.

Purpose: After the code deletions in Plans 01-04, some remaining tests may reference deleted code or fail. This plan fixes or deletes those tests, ensures the codebase passes typecheck and biome, and proves main text sync still works.

Output: Clean codebase that passes all checks, with integration test proving main text sync.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix yjsAnnotations.test.ts for stripped plugin</name>
  <files>src/lib/collab/yjsAnnotations.test.ts</files>
  <read_first>
    - src/lib/collab/yjsAnnotations.test.ts (the file being modified)
    - src/lib/collab/yjsAnnotations.ts (to understand what's left to test)
  </read_first>
  <action>
Read `src/lib/collab/yjsAnnotations.test.ts` and identify tests that:
1. Test the write path (CM -> Yjs) which is now disabled
2. Use deleted imports or functions

For tests that test the WRITE path (local CM changes syncing to Yjs):
- These tests will fail because the update() method now returns early
- DELETE these tests or mark them as `it.skip` with a comment "Phase 10: write path disabled"

For tests that test the READ path (Yjs changes syncing to CM):
- These should still work because the deepObserver is preserved
- KEEP these tests

For tests that test initial sync (`_syncInitialToYjs`, `_syncInitialFromYjs`):
- These should still work
- KEEP these tests

For any test that uses imports that no longer exist:
- Update imports or delete the test

Add a file-level comment:
```typescript
/**
 * yjsAnnotations.test.ts -- Tests for annotation sync plugin.
 *
 * Phase 10: Write path (CM -> Yjs) disabled. Tests for write path skipped.
 * Phase 11 will rebuild and re-enable these tests.
 */
```
  </action>
  <verify>
    <automated>bun run test:run src/lib/collab/yjsAnnotations.test.ts 2>&1 | grep -q "passed" && echo "Tests pass"</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test:run src/lib/collab/yjsAnnotations.test.ts` passes (all tests pass or are skipped)
    - Write path tests are either deleted or marked `.skip`
    - Read path and initial sync tests still run and pass
    - No import errors
  </acceptance_criteria>
  <done>yjsAnnotations.test.ts updated for stripped plugin</done>
</task>

<task type="auto">
  <name>Task 2: Fix or delete yjsAnnotations.convergence.test.ts</name>
  <files>src/lib/collab/yjsAnnotations.convergence.test.ts</files>
  <read_first>
    - src/lib/collab/yjsAnnotations.convergence.test.ts (the file being modified)
  </read_first>
  <action>
Read `src/lib/collab/yjsAnnotations.convergence.test.ts` and determine:
- Does it test annotation sync between two peers?
- If yes, these tests will fail because write path is disabled

Options:
1. If all tests in this file test the write path, DELETE the file entirely
2. If some tests test read-back or initial sync, keep those and delete/skip write tests

Most likely this file tests two-peer annotation convergence which requires the write path. In that case, DELETE the entire file:

```bash
rm src/lib/collab/yjsAnnotations.convergence.test.ts
```

Add a note in the commit message: "Phase 10: yjsAnnotations.convergence.test.ts deleted - tests write path which is disabled. Phase 13 will create new convergence tests."
  </action>
  <verify>
    <automated>! ls src/lib/collab/yjsAnnotations.convergence.test.ts 2>/dev/null || bun run test:run src/lib/collab/yjsAnnotations.convergence.test.ts 2>&1 | grep -q "passed"</automated>
  </verify>
  <acceptance_criteria>
    - Either file is deleted OR all remaining tests pass
    - No tests fail due to disabled write path
  </acceptance_criteria>
  <done>yjsAnnotations.convergence.test.ts handled (deleted or fixed)</done>
</task>

<task type="auto">
  <name>Task 3: Run typecheck and biome, fix any errors</name>
  <files>Multiple files may need fixes</files>
  <read_first>
    - (None - run commands first to find errors)
  </read_first>
  <action>
Run typecheck:
```bash
bun run check
```

If there are TypeScript errors:
1. Unused imports from deleted code - remove them
2. Type mismatches from function signature changes - fix them
3. Missing exports that were deleted - remove references

Run biome:
```bash
bun run biome
```

If there are linting errors:
1. Unused variables - remove them
2. Style issues - fix them

Common expected errors to fix:
- Unused `import type * as Y` after removing collab code
- Unused `collabSession` imports in Svelte files
- Unused function parameters that were for collab (rename to `_param`)
- References to deleted exports in other files

Iterate: run check, fix errors, run check again until clean.
  </action>
  <verify>
    <automated>bun run check && bun run biome && echo "All checks pass"</automated>
  </verify>
  <acceptance_criteria>
    - `bun run check` exits 0 with no errors
    - `bun run biome` exits 0 with no errors
    - All TypeScript imports resolve
    - No unused imports or variables
  </acceptance_criteria>
  <done>Typecheck and biome pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Test and lint fixes only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | N/A | No production logic changes |
</threat_model>

<verification>
After all tasks:
```bash
# Full check
bun run check && bun run biome && bun run test:run src/lib/collab/ && echo "All collab tests pass"
```
</verification>

<success_criteria>
- [ ] `bun run check` passes with no errors
- [ ] `bun run biome` passes with no errors
- [ ] All remaining collab tests pass
- [ ] yjsAnnotations.test.ts passes (write tests skipped/deleted)
- [ ] yjsAnnotations.convergence.test.ts deleted or passes
</success_criteria>

<output>
After completion, create `.planning/phases/10-strip-broken-collab-sync-layer/10-05-SUMMARY.md`
</output>
