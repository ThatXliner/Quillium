---
phase: 04-relay-core
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - quillium-landing/relay/package.json
  - quillium-landing/relay/tsconfig.json
  - quillium-landing/relay/vitest.config.ts
  - quillium-landing/relay/src/index.ts
  - quillium-landing/relay/src/__tests__/auth.test.ts
  - quillium-landing/relay/src/__tests__/room.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Relay directory exists with TypeScript project scaffold"
    - "Test infrastructure ready with vitest"
    - "Test stubs exist for all requirements"
    - "Type checking passes"
  artifacts:
    - path: "quillium-landing/relay/package.json"
      provides: "Project manifest with dependencies"
      contains: "socket.io"
    - path: "quillium-landing/relay/tsconfig.json"
      provides: "TypeScript configuration"
      contains: "strict"
    - path: "quillium-landing/relay/vitest.config.ts"
      provides: "Test configuration"
      contains: "vitest"
    - path: "quillium-landing/relay/src/__tests__/auth.test.ts"
      provides: "Auth test stubs"
      contains: "RELY-01"
    - path: "quillium-landing/relay/src/__tests__/room.test.ts"
      provides: "Room test stubs"
      contains: "RELY-03"
  key_links:
    - from: "quillium-landing/relay/vitest.config.ts"
      to: "quillium-landing/relay/src/__tests__/*.test.ts"
      via: "test discovery"
      pattern: "include.*test"
---

<objective>
Create the relay server project scaffold in the quillium-landing repository.

Purpose: Establish the Wave 0 foundation before implementing features. Creates the TypeScript project structure, installs dependencies, and sets up test infrastructure with stubs for all requirements.

Output: `quillium-landing/relay/` directory with package.json, TypeScript config, vitest setup, and test stubs.
</objective>

<execution_context>
@/Users/bryanhu/Developer/current/Quillium/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bryanhu/Developer/current/Quillium/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-relay-core/04-RESEARCH.md
@.planning/phases/04-relay-core/04-PATTERNS.md

<interfaces>
<!-- The relay is a NEW project - no existing interfaces to reference -->
<!-- Use RESEARCH.md patterns for structure -->

From 04-RESEARCH.md Standard Stack:
```
socket.io: 4.8.3 - WebSocket server
@supabase/supabase-js: 2.103.3 - JWT validation
@codemirror/collab: 6.1.1 - OT utilities
@codemirror/state: 6.6.0 - ChangeSet types
zod: 4.3.6 - Message validation
typescript: ~5.9 - Type safety
vitest: 4.x - Testing
```

From 04-RESEARCH.md Recommended Project Structure:
```
relay/
├── src/
│   ├── index.ts          # Server entry point
│   ├── server.ts         # Socket.io server setup
│   ├── auth/
│   ├── rooms/
│   ├── handlers/
│   └── schemas.ts
├── tests/
├── package.json
├── tsconfig.json
└── vitest.config.ts
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create relay directory and package.json with dependencies</name>
  <files>quillium-landing/relay/package.json</files>
  <read_first>
    - /Users/bryanhu/Developer/current/quillium-landing/package.json (parent project conventions)
    - /Users/bryanhu/Developer/current/Quillium/.planning/phases/04-relay-core/04-RESEARCH.md (dependency versions)
  </read_first>
  <action>
Create the `relay/` directory in quillium-landing and initialize package.json:

```bash
cd /Users/bryanhu/Developer/current/quillium-landing
mkdir -p relay/src/__tests__ relay/src/auth relay/src/rooms relay/src/handlers
```

Create `relay/package.json`:
```json
{
  "name": "quillium-relay",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "vitest",
    "test:run": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "socket.io": "^4.8.3",
    "@supabase/supabase-js": "^2.103.3",
    "@codemirror/collab": "^6.1.1",
    "@codemirror/state": "^6.6.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "@types/node": "^22.0.0",
    "vitest": "^4.1.4",
    "tsx": "^4.19.0",
    "dotenv": "^16.5.0"
  }
}
```

Then install dependencies:
```bash
cd /Users/bryanhu/Developer/current/quillium-landing/relay
bun install
```

Note: The parent quillium-landing uses bun per project conventions.
  </action>
  <acceptance_criteria>
    - Directory `/Users/bryanhu/Developer/current/quillium-landing/relay/` exists
    - `relay/package.json` contains `"socket.io": "^4.8.3"`
    - `relay/package.json` contains `"@supabase/supabase-js": "^2.103.3"`
    - `relay/package.json` contains `"@codemirror/collab": "^6.1.1"`
    - `relay/package.json` contains `"vitest": "^4`
    - `relay/package.json` has `"type": "module"` for ESM
    - `relay/node_modules/` exists after install
  </acceptance_criteria>
  <verify>
    <automated>test -f /Users/bryanhu/Developer/current/quillium-landing/relay/package.json && grep -q '"socket.io"' /Users/bryanhu/Developer/current/quillium-landing/relay/package.json && grep -q '"vitest"' /Users/bryanhu/Developer/current/quillium-landing/relay/package.json && echo "package.json created"</automated>
  </verify>
  <done>relay/package.json exists with all required dependencies installed</done>
</task>

<task type="auto">
  <name>Task 2: Create TypeScript configuration</name>
  <files>quillium-landing/relay/tsconfig.json</files>
  <read_first>
    - /Users/bryanhu/Developer/current/Quillium/tsconfig.json (project TypeScript patterns)
  </read_first>
  <action>
Create `relay/tsconfig.json`:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "lib": ["ES2022"],
    "outDir": "dist",
    "rootDir": "src",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": false
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist", "**/*.test.ts"]
}
```

Key choices:
- `module: NodeNext` for ESM compatibility with `"type": "module"`
- `strict: true` per D-31 (TypeScript with type safety)
- `outDir: dist` for compiled output (used by Dockerfile)
- Excludes test files from build output
  </action>
  <acceptance_criteria>
    - `relay/tsconfig.json` exists
    - Contains `"strict": true`
    - Contains `"module": "NodeNext"`
    - Contains `"target": "ES2022"`
    - Contains `"outDir": "dist"`
  </acceptance_criteria>
  <verify>
    <automated>grep -q '"strict": true' /Users/bryanhu/Developer/current/quillium-landing/relay/tsconfig.json && grep -q '"module": "NodeNext"' /Users/bryanhu/Developer/current/quillium-landing/relay/tsconfig.json && echo "tsconfig created"</automated>
  </verify>
  <done>tsconfig.json exists with strict mode enabled</done>
</task>

<task type="auto">
  <name>Task 3: Create vitest configuration and test stubs</name>
  <files>quillium-landing/relay/vitest.config.ts, quillium-landing/relay/src/__tests__/auth.test.ts, quillium-landing/relay/src/__tests__/room.test.ts</files>
  <read_first>
    - /Users/bryanhu/Developer/current/Quillium/vitest.config.ts (vitest configuration pattern)
    - /Users/bryanhu/Developer/current/Quillium/.planning/phases/04-relay-core/04-VALIDATION.md (test requirements)
  </read_first>
  <action>
Create `relay/vitest.config.ts`:
```typescript
import { defineConfig } from "vitest/config";

export default defineConfig({
    test: {
        include: ["src/**/*.test.ts"],
        environment: "node",
        globals: true,
        coverage: {
            provider: "v8",
            include: ["src/**/*.ts"],
            exclude: ["src/**/*.test.ts", "src/index.ts"],
        },
    },
});
```

Create `relay/src/__tests__/auth.test.ts`:
```typescript
/**
 * auth.test.ts -- Tests for JWT authentication and permission validation.
 *
 * Covers RELY-01 (JWT auth) and RELY-02 (permission validation).
 */
import { describe, it, expect } from "vitest";

describe("RELY-01: JWT Authentication", () => {
    it.todo("rejects connection without token");
    it.todo("rejects connection with invalid token");
    it.todo("rejects connection with expired token");
    it.todo("accepts connection with valid Supabase JWT");
    it.todo("extracts user ID from valid token");
});

describe("RELY-02: Permission Validation", () => {
    it.todo("rejects connection when user has no access to document");
    it.todo("accepts connection when user has share access");
    it.todo("accepts connection when user is document owner");
    it.todo("caches permission check for session duration");
});
```

Create `relay/src/__tests__/room.test.ts`:
```typescript
/**
 * room.test.ts -- Tests for room management and OT ordering.
 *
 * Covers RELY-03 (version ordering) and RELY-04 (broadcasting).
 */
import { describe, it, expect } from "vitest";

describe("RELY-03: Version Ordering (rebaseUpdates)", () => {
    it.todo("accepts updates at current version");
    it.todo("rebases updates from stale client version");
    it.todo("assigns monotonically increasing version numbers");
    it.todo("applies rebased changes to document state");
});

describe("RELY-04: Update Broadcasting", () => {
    it.todo("broadcasts accepted updates to all room members");
    it.todo("excludes sender from broadcast");
    it.todo("includes version number in broadcast");
});

describe("Room Lifecycle", () => {
    it.todo("creates room on first client connection");
    it.todo("schedules cleanup when last client disconnects");
    it.todo("cancels cleanup when new client joins before timeout");
    it.todo("deletes room after cleanup timeout with no clients");
});
```
  </action>
  <acceptance_criteria>
    - `relay/vitest.config.ts` exists with `environment: "node"`
    - `relay/src/__tests__/auth.test.ts` contains `describe("RELY-01`
    - `relay/src/__tests__/auth.test.ts` contains `describe("RELY-02`
    - `relay/src/__tests__/room.test.ts` contains `describe("RELY-03`
    - `relay/src/__tests__/room.test.ts` contains `describe("RELY-04`
    - All tests use `it.todo()` stubs (no implementation yet)
    - `bun run test:run` exits 0 (todo tests are skipped, not failed)
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'RELY-01' /Users/bryanhu/Developer/current/quillium-landing/relay/src/__tests__/auth.test.ts && grep -q 'RELY-03' /Users/bryanhu/Developer/current/quillium-landing/relay/src/__tests__/room.test.ts && cd /Users/bryanhu/Developer/current/quillium-landing/relay && bun run test:run 2>&1 | tail -5</automated>
  </verify>
  <done>vitest configured, test stubs created for all four requirements</done>
</task>

<task type="auto">
  <name>Task 4: Create minimal entry point for type checking</name>
  <files>quillium-landing/relay/src/index.ts</files>
  <read_first>
    - /Users/bryanhu/Developer/current/Quillium/.planning/phases/04-relay-core/04-PATTERNS.md (entry point pattern)
  </read_first>
  <action>
Create `relay/src/index.ts`:
```typescript
/**
 * index.ts -- Relay server entry point.
 *
 * WebSocket relay server for Quillium Omni real-time collaboration.
 * Handles JWT authentication, permission validation, OT ordering,
 * and update broadcasting between connected clients.
 *
 * See: .planning/phases/04-relay-core/04-RESEARCH.md for architecture.
 */

const PORT = process.env.PORT ?? 3001;

console.log(`[relay] Starting on port ${PORT}...`);

// Placeholder for server implementation (Plan 02/03)
export {};
```

Verify type checking passes:
```bash
cd /Users/bryanhu/Developer/current/quillium-landing/relay
bun run typecheck
```
  </action>
  <acceptance_criteria>
    - `relay/src/index.ts` exists
    - Contains module-level JSDoc comment
    - Contains `process.env.PORT` reference
    - `bun run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>test -f /Users/bryanhu/Developer/current/quillium-landing/relay/src/index.ts && cd /Users/bryanhu/Developer/current/quillium-landing/relay && bun run typecheck && echo "typecheck passed"</automated>
  </verify>
  <done>Entry point exists, type checking passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Wave 0 is scaffold only - no runtime code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-00 | Tampering | dependencies | mitigate | Pin dependency versions in package.json; lockfile committed |
</threat_model>

<verification>
```bash
# Verify project structure
ls /Users/bryanhu/Developer/current/quillium-landing/relay/

# Verify package.json has required deps
grep -q "socket.io" /Users/bryanhu/Developer/current/quillium-landing/relay/package.json
grep -q "@codemirror/collab" /Users/bryanhu/Developer/current/quillium-landing/relay/package.json

# Verify TypeScript config
grep -q '"strict": true' /Users/bryanhu/Developer/current/quillium-landing/relay/tsconfig.json

# Verify test stubs exist
grep -q "RELY-01" /Users/bryanhu/Developer/current/quillium-landing/relay/src/__tests__/auth.test.ts
grep -q "RELY-03" /Users/bryanhu/Developer/current/quillium-landing/relay/src/__tests__/room.test.ts

# Verify type checking passes
cd /Users/bryanhu/Developer/current/quillium-landing/relay && bun run typecheck

# Verify tests run (todos are skipped)
cd /Users/bryanhu/Developer/current/quillium-landing/relay && bun run test:run
```
</verification>

<success_criteria>
- `quillium-landing/relay/` directory exists with proper structure
- package.json has all required dependencies (socket.io, @supabase/supabase-js, @codemirror/collab, zod, vitest)
- tsconfig.json configured for strict TypeScript with ESM
- vitest.config.ts ready for test execution
- Test stubs exist for RELY-01, RELY-02, RELY-03, RELY-04
- Type checking passes
- Tests run without errors (todos are skipped)
</success_criteria>

<output>
After completion, create `.planning/phases/04-relay-core/04-01-SUMMARY.md`
</output>
