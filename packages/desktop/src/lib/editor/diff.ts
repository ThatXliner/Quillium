/**
 * diff.ts — Compatibility re-export for the shared word-level diff.
 *
 * Desktop suggestions and history, Omni Web Preview, and future read-only
 * surfaces must use the same algorithm. Add diff behavior in @quillium/share.
 */
export * from "@quillium/share/diff";
