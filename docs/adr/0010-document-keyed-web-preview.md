# Keep public Web Preview identity document-keyed

Key a public Web Preview's identity and URL by document while its versioned
payload carries the chosen tabs and their selected drafts. Publishing replaces
the document's shared snapshot instead of minting draft-specific links, which
keeps the link stable as the writer moves between tabs and runs.
