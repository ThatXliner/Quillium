import { describe, it } from "vitest";

// Covered by Playwright in tests/e2e/nestedRevisionRouting.pw.ts.
// Keeping this file as an explicit placeholder avoids losing the historical
// intent while steering future updates toward the browser-level regression.
describe.skip("nested annotation creation routing", () => {
    it("is covered by Playwright", () => {});
});
